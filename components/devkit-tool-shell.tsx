'use client'
import Link from 'next/link'
import { ArrowLeft, Check, Clipboard, Eraser, LockKeyhole } from 'lucide-react'
import { useState, type ReactNode } from 'react'
export function ToolShell({ title, description, children }: { title: string; description: string; children: ReactNode }) { return <main className="min-h-screen bg-background text-foreground"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="font-mono text-sm font-bold tracking-tight">DEVKIT</Link><span className="flex items-center gap-2 text-xs text-muted-foreground"><LockKeyhole className="size-3.5 text-accent" /> Processed locally</span></header><div className="mx-auto max-w-6xl px-5 pb-20 pt-10 lg:px-8"><Link href="/tools" className="mb-8 inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Back to tools</Link><h1 className="text-4xl font-semibold tracking-tight">{title}</h1><p className="mt-3 text-muted-foreground">{description}</p>{children}</div></main> }
export function Actions({ output, onCopy, onClear, download }: { output: string; onCopy: () => void; onClear: () => void; download?: () => void }) { const [copied, setCopied] = useState(false); const copy = async () => { if (!output) return; await navigator.clipboard.writeText(output); setCopied(true); setTimeout(() => setCopied(false), 1200); onCopy() }; return <div className="mt-4 flex flex-wrap gap-2"><button onClick={copy} disabled={!output} className="inline-flex items-center gap-2 rounded-md bg-primary px-3 py-2 text-sm text-primary-foreground disabled:opacity-40">{copied ? <Check className="size-4" /> : <Clipboard className="size-4" />} {copied ? 'Copied' : 'Copy'}</button>{download && <button onClick={download} disabled={!output} className="rounded-md border border-border px-3 py-2 text-sm disabled:opacity-40">Download</button>}<button onClick={onClear} className="inline-flex items-center gap-2 rounded-md border border-border px-3 py-2 text-sm"><Eraser className="size-4" /> Clear</button></div> }
export function TextArea({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) { return <label className="flex min-h-72 flex-col overflow-hidden rounded-lg border border-border bg-card"><span className="border-b border-border px-4 py-3 font-mono text-xs uppercase tracking-wider text-muted-foreground">{label}</span><textarea aria-label={label} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} spellCheck={false} className="min-h-64 flex-1 resize-y bg-background p-4 font-mono text-sm leading-6 outline-none" /></label> }
