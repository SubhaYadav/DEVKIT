'use client'

import Link from 'next/link'
import { useMemo, useState } from 'react'
import { ArrowLeft, Code2, Search } from 'lucide-react'
import { allTools } from '@/lib/devkit/registry'

export default function ToolsPage() { const [query,setQuery]=useState(''); const filtered=useMemo(()=>allTools.filter(t=>`${t.name} ${t.description} ${t.category} ${t.keywords.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[query]); return <main className="min-h-screen bg-background text-foreground"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="flex items-center gap-3 font-mono text-sm font-bold"><span className="grid size-8 place-items-center rounded-lg bg-primary text-primary-foreground"><Code2 className="size-4" /></span>DEVKIT</Link><Link href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Home</Link></header><div className="mx-auto max-w-6xl px-5 pb-20 pt-12 lg:px-8"><p className="font-mono text-xs uppercase tracking-[0.2em] text-accent">Directory</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">All tools</h1><p className="mt-3 text-muted-foreground">Focused utilities that process your input locally.</p><div className="relative mt-9 max-w-xl"><Search className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input aria-label="Search tools" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search tools..." className="h-12 w-full rounded-lg border border-border bg-card pl-11 text-sm outline-none focus:border-accent" /></div><p className="mt-4 font-mono text-xs text-muted-foreground">{filtered.length} tools</p><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(tool=><Link key={tool.id} href={tool.route} className="rounded-lg border border-border bg-card p-5 transition-colors hover:border-accent/60"><span className="font-mono text-[10px] uppercase tracking-wider text-accent">{tool.category}</span><h2 className="mt-4 font-medium">{tool.name}</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">{tool.description}</p><span className="mt-5 inline-block font-mono text-[10px] text-muted-foreground">{tool.keywords.join(' · ')}</span></Link>)}</div>{!filtered.length&&<p className="py-10 text-sm text-muted-foreground">No tools match your search.</p>}</div></main> }
