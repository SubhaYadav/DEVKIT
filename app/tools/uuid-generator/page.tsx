'use client'
import { useState } from 'react'
import { Actions, ToolShell } from '@/components/devkit-tool-shell'
import { createUuids } from '@/lib/devkit/uuid'
export default function UuidPage(){const [count,setCount]=useState(1);const [items,setItems]=useState<string[]>([]);const [error,setError]=useState('');const output=items.join('\n');const generate=()=>{try{setItems(createUuids(count));setError('')}catch(e){setError(e instanceof Error?e.message:'Unable to generate UUIDs.')}};const download=()=>{const blob=new Blob([output+'\n'],{type:'text/plain'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='uuids.txt';a.click();URL.revokeObjectURL(url)};return <ToolShell title="UUID v4 Generator" description="Generate cryptographically secure UUID version 4 values in your browser."><div className="mt-8 flex flex-wrap items-end gap-3"><label className="flex flex-col gap-2 text-xs text-muted-foreground">Quantity<select value={count} onChange={e=>setCount(Number(e.target.value))} className="h-10 rounded-md border border-border bg-card px-3 text-sm text-foreground"><option value="1">1</option><option value="5">5</option><option value="10">10</option><option value="50">50</option><option value="100">100</option></select></label><button onClick={generate} className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground">Generate</button></div><div className="mt-4 rounded-lg border border-border bg-card"><div className="border-b border-border px-4 py-3 font-mono text-xs uppercase tracking-wider text-muted-foreground">Generated UUIDs</div><pre className="min-h-64 whitespace-pre-wrap bg-background p-4 font-mono text-sm leading-7">{output}</pre>{error&&<p className="border-t border-destructive/30 bg-destructive/10 px-4 py-3 text-xs text-destructive">{error}</p>}</div><Actions output={output} onCopy={()=>{}} onClear={()=>setItems([])} download={items.length>1?download:undefined}/></ToolShell>}
